#!/bin/sh
# Refuse to push code that does not build or vet. Enable once per clone:
#   git config core.hooksPath .githooks
set -e
cd "$(git rev-parse --show-toplevel)"
go build ./... && go vet ./... && go test ./harness ./runner ./server >/dev/null
