name: goose

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    uses: ./.github/workflows/test-harness.yml
    with:
      harness: goose
      modes: '["headless","acp"]'
