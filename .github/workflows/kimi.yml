name: kimi

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    uses: ./.github/workflows/test-harness.yml
    with:
      harness: kimi
      modes: '["headless","acp"]'
